# Microsoft OAuth2 client credentials
CLIENT_ID=your-client-id
CLIENT_SECRET=your-client-secret
TENANT_ID=your-tenant-id
# Scope for token request (example: API scope or .default scope)
SCOPE=https://graph.microsoft.com/.default

# Base URL for the API under test (optional for offline tests)
API_BASE_URL=https://api.example.com
# Local token to use instead of calling the OAuth provider (useful for local testing)
#LOCAL_TOKEN=eyJhbGciOi... (paste a bearer token here to skip OAuth)
